Expand the following plan into atomic, executable test cases suitable for an automation runner.

Return JSON array, each item with:
- "name": short title
- "description": what to validate
- "input_data": optional dict of parameters or fixtures
- "execution_order": integer ordering (start at 1)

High-level goal:
{high_level_goal}

Plan summary (may be JSON):
{plan_summary}
